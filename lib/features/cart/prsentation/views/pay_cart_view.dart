import 'package:flutter/material.dart';

class PayCartView extends StatelessWidget {
  const PayCartView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
